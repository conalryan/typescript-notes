import { designPatternsObserver } from './design-patterns-observer';

describe('designPatternsObserver', () => {
  it('should work', () => {
    expect(designPatternsObserver()).toEqual('design-patterns-observer');
  });
});
