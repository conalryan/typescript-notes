import { designPatternsFunctionalProgramming } from './design-patterns-functional-programming';

describe('designPatternsFunctionalProgramming', () => {
  it('should work', () => {
    expect(designPatternsFunctionalProgramming()).toEqual(
      'design-patterns-functional-programming'
    );
  });
});
