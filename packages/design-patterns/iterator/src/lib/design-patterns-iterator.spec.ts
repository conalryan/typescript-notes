import { designPatternsIterator } from './design-patterns-iterator';

describe('designPatternsIterator', () => {
  it('should work', () => {
    expect(designPatternsIterator()).toEqual('design-patterns-iterator');
  });
});
