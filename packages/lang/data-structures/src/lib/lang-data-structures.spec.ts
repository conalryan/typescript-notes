import { langDataStructures } from './lang-data-structures';

describe('langDataStructures', () => {
  it('should work', () => {
    expect(langDataStructures()).toEqual('lang-data-structures');
  });
});
